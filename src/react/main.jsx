import React from "react";
import ReactDOM from "react-dom/client";
import BeautyHome from "./BeautyHome.jsx";

ReactDOM.createRoot(document.getElementById("beauty-root")).render(
    <React.StrictMode>
        <BeautyHome />
    </React.StrictMode>
);